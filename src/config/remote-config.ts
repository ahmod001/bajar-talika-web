import { info } from "../../public/app/app.info";
/**
 * Remote config is used in the app,
 * to send some parameters dynamically from here.
 */

export const config = {
    app_version: info.app_version,

} as const